<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/detail.css">
    <title><%= item.name %></title>
</head>
<body>
    <div class="container">
        <h1><%= item.name %></h1>

        <div class="detail-card">
            <h2>Description</h2>
            <p><%= item.description %></p>
        </div>

        <div class="detail-card">
            <h2>Details</h2>
            <p><strong>Price:</strong> ₹<%= item.price %></p>
            <p><strong>Quantity:</strong> <%= item.quantity %></p>
            <p><strong>Category:</strong> <a href="/categories/<%= item.category._id %>"><%= item.category.name %></a></p>
        </div>

        <div class="actions">
            <form method="GET" action="/items/<%= item._id %>/update">
                <button type="submit" class="edit-btn" onClick = "return confirm('Do you want to edit this Item? Please enter the Admin Password')">Edit Item</button>
            </form>
            
            <form method="POST" action="/items/<%= item._id %>/delete" class="delete-row">
               <button type="submit" class="delete-btn" onClick = "return confirm('Do you want to delete this Item? Please enter the Admin Password')">Delete Item</button>
               <input type="password" name="adminPassword" placeholder="Admin Password" class="admin-inline-password"required/>
            </form>

            <a href="/items" class="back-link">Back to Items</a>
        </div>
    </div>
</body>
</html>
